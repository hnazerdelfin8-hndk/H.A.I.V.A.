plugins {
    id 'com.android.application'
}

android {
    namespace 'com.haiva.assistant'
    compileSdk 35

    defaultConfig {
        applicationId 'com.haiva.assistant'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    testImplementation 'junit:junit:4.13.2'
}
