export const V3_STATES=Object.freeze({IDLE:"IDLE",ARMED:"ARMED",COMMITTED:"COMMITTED"});
export function createVoiceInteractionV3(){let generation=0,state=V3_STATES.IDLE,monitoringTurn=null,committed=false;const beginTurn=()=>{generation+=1;committed=false;state=V3_STATES.IDLE;monitoringTurn=null;return generation;};const beginMonitoring=(turn=generation)=>{if(turn!==generation)return false;monitoringTurn=turn;state=V3_STATES.ARMED;committed=false;return true;};const stopMonitoring=(turn=generation)=>{if(monitoringTurn!==null&&turn!==monitoringTurn)return false;monitoringTurn=null;state=V3_STATES.IDLE;committed=false;return true;};const getState=()=>state;const isMonitoring=turn=>state===V3_STATES.ARMED&&monitoringTurn===turn;const isCurrent=turn=>turn===generation;const commitCapture=(turn,text)=>{if(!isMonitoring(turn)||committed)return null;const value=String(text??"").trim();if(!value)return null;committed=true;state=V3_STATES.COMMITTED;return Object.freeze({turn,text:value,source:"v3"});};const releaseCapture=(turn=generation)=>{if(!isCurrent(turn))return false;committed=false;if(monitoringTurn===turn)state=V3_STATES.ARMED;return true;};return Object.freeze({beginTurn,beginMonitoring,stopMonitoring,getState,isMonitoring,isCurrent,commitCapture,releaseCapture});}
